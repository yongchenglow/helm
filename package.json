{
	"name": "helm",
	"version": "1.0.0",
	"private": true,
	"description": "Helm charts deployed to the personal Kubernetes cluster",
	"license": "ISC",
	"scripts": {
		"prepare": "husky",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"update:packages": "bun update --latest",
		"helm:deps": "helm dependency update ./metrics-server && helm dependency update ./external-dns",
		"helm:lint": "helm lint ./metrics-server ./external-dns",
		"helm:template": "helm template metrics-server ./metrics-server > /dev/null && helm template external-dns ./external-dns > /dev/null",
		"check": "bun run lint && bun run helm:lint",
		"fix:all": "bun run lint:fix && bun run helm:lint"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.11",
		"@commitlint/cli": "^21.2.2",
		"@commitlint/config-conventional": "^21.2.2",
		"husky": "^9.1.7"
	}
}
